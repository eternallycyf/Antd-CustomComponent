import { Page } from '@/components/CommonCard';
import { Button } from 'antd';

const Add: React.FC = () => {
  return (
    <Page>
      <Button>添加权限</Button>
    </Page>
  );
};

export default Add;
